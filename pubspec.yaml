name: untitled7
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^0.1.3
  google_fonts: ^1.1.0
  flutter_secure_storage: ^4.1.0

  sqflite:
  path:

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  fonts:
    - family: Manrope
      fonts:
        - asset: assets/fonts/static/Manrope-Regular.ttf

  uses-material-design: true

  assets:
    - assets/images/